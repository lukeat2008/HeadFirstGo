1) Go methods: functions that are associated with values of a given type.
  
   Call method syntax : value dot method_name
   
   Difference between function and method:
      function belongs to a package
      method belongs to a value

2) Comment: Everything from the double slashes (//) to the end of the line is treated as part of the comment. 

3) In Go, functions and methods can return any number of values.

4) Blank identifier, a single underscore ( _ )
   just for leave a position for the variable
   When we have a value that would normally be assigned to a variable, but that we don't intend to use.
   
5) 
